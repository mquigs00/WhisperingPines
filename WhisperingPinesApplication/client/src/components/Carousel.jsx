import CarouselCSS from './Carousel.module.css';

const Carousel = () => {
    /*
    Currently just going to use 24 of the books that I have cover images for to put in the carousel.
    In the future once I load more books and have the basic site set up I
    will improve the carousel to dynamically query the RDS database to get the 20 most recent
    books that have been inserted and use them as the true Newest Arrivals 
    */
    return (
        <div>
            
        </div>
    )
}

export default Carousel;